# claude-company

> Experimenting with Claude Code, I want to build a tool for myself to manage our division workforce without writing any code.

A web application for importing and viewing employee data from CSV files. The sample [employees.csv](employees.csv) was randomly generated by 
Claude and rolls up to the CEO "Peter Parker." 

Use cases (basic)

- A master CSV file determines the current division employees provided by our HR software

- Import CSV file - Allows to see what changed from the last import

- Team re-organization planning - Allows to drag-and-drop and move teammates around and export to CSV file


## Prerequisites

- Python 3.8+

```
sudo alternatives --set python /usr/bin/python3.12
sudo alternatives --set python3 /usr/bin/python3.12
```

## Setup

Create and activate a virtual environment, then install dependencies:

```bash
/usr/bin/python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Run

With the virtual environment activated:

```bash
source venv/bin/activate
python app.py
```

Then open **http://localhost:8080** in your browser.

## Deactivate

```bash
deactivate
```

## Container

### Prerequisites

- Podman / Docker

### Build

```bash
podman build -t company .
```

### Run

```bash
podman run --rm -p 8080:8080 company
```

Then open **http://localhost:8080** in your browser.

To persist employee data across container restarts, mount the data directory:

```bash
podman run --rm \
    --privileged \
    -p 8080:8080 \
    -v $(pwd)/data:/app/data company
```

## Usage

1. **Upload CSV** — select your employee CSV file and click **Import Employees**
2. **View Employees** — browse the imported records; expand the Attributes column for additional fields
3. Navigate to the other screens to see the organization workforce

## Notes

- The app replaces the employee store on each upload
- Employee data is saved to `data/employees.json` (created automatically on first import)

## Claude Instruction History

> Claude command: show instruction history

### Phase 1 - Training Claude and basic app functionality

> Ramping up on Claude and getting my development environment up (4 hours) - Account setup, Calude settings, dotfiles, dev containers

> Training and lettting Claude work (12-14 hours) - Basic app functionality, container deployment, packaging

1. Analyze CLAUDE.md and build application — created app.py (Flask), templates/base.html, upload.html, employees.html, and requirements.txt
2. Update .gitignore for project — added Python, app data, and editor patterns
3. Generate instructions on how to run on laptop — provided setup/run steps in chat
4. Generate pip.conf for project — created pip.conf pointing to PyPI with trusted hosts
5. Add an Employee Details view — added /employees/<employee_id> route, employee_detail.html template, and linked Employee ID/Name columns
6. Add "Details" button for each employee — added a Details button in the last column of the employee table linking to the detail view
7. Create "Organization" view — added /organization route, organization.html tree view grouped by Supervisory Organization with expand/collapse controls and Details buttons
8. Add "View Chart" button to Organization view — added /organization/chart route, organization_chart.html SVG-based org chart with zoom, pan, connector lines, and clickable employee cards
9. Link supervisor via employee ID in supervisor_organization — added extract_supervisor_id() and build_org_tree() helpers; rebuilt Organization view as recursive hierarchy tree; rebuilt chart as top-down hierarchy with bezier edges; linked supervisor on Employee Details page
10. Add sortable columns in "View Employees" screen — added click-to-sort on all 10 data columns with alphabetic and numeric sorting, ascending/descending toggle, and sort direction indicators
11. Create a Dockerfile — added Dockerfile using python:3.12-slim, added data/.gitkeep
12. Add a search bar at the top navigation — added /search route, search.html results template, and a persistent search form in the header searching across Employee ID, Name, Job Profile, Supervisory Org, and Location
13. Include a "Back to Search Results" button — passed from_search query param through detail links; detail page shows "Back to Search Results" or "Back to Employees" conditionally
14. Add "Export to CSV" from Employees view — added /employees/export route that reconstructs CSV with original headers and expanded attributes; added green Export to CSV button to toolbar
15. Format Hourly Rate, Salary Min, Salary Mid, Salary Max as currency — added currency Jinja2 filter in app.py formatting values as $1,234.56; applied in employees.html and employee_detail.html
16. Remove currency columns from Employees view — removed Hourly Rate, Salary Min, Salary Mid, Salary Max columns from the employee table
17. Show only Employee ID, Name, Job Profile, Supervisory Org, Location in Employees view — removed Annual Salary and Attributes columns; kept Details button
18. Add "Show Salary" button to Employees view — added toggle button; Annual Salary column hidden/shown via CSS display toggle
19. Include Annual Salary column always visible but blurred until "Show Salary" is clicked — replaced display toggle with CSS blur filter; values unblur on button click with smooth transition
20. Disable "Annual Salary" sort until "Show Salary" is toggled — salary column header dimmed and non-clickable by default; sort enabled/disabled in sync with the Show/Hide Salary toggle
21. Allow preview of CSV import before committing — added /upload/preview endpoint; upload screen shows Preview tab (first 10 rows) and file selection triggers fetch to preview endpoint
22. Provide a diff screen to preview changes from current state — /upload/preview now computes added/removed/modified/unchanged diff; Changes tab shows git-style diff with color-coded blocks and per-field old/new lines
23. Allow moving employees to different supervisor on Organization screen — added drag-and-drop to employee cards; moves tracked client-side with undo per move and reset all; pending changes panel shows at bottom-right
24. Export CSV from Organization screen with current reassignments — added /organization/export POST endpoint; applies moves to a deep copy of employee data and returns updated CSV with new supervisor_organization values
25. Blur Annual Salary in Organization view with Show Salary toggle — added salary-value span with CSS blur to each employee card; Show/Hide Salary button in toolbar reveals/re-blurs all values with smooth transition
26. Allow delete when moving employees around the "Organization" screen but do not persist data
27. Blur Annual Salary in Search Results view with Show Salary toggle — applied CSS blur to salary-value span; Show/Hide Salary toggle button in toolbar; currency filter applied to salary display
28. Hide Annual Salary in Preview Import tab — salary column cells blurred by default with inline Show/Hide Salary toggle button next to the row count in the preview meta line
29. Add "Import Example" button to upload screen — added /upload/example POST route that loads employees.csv from the project root and imports it directly; button appears below the file input on the upload card

### Phase 2 - Add staffing functionality

1. Analyze CLAUDE.md and code — Explored full codebase; confirmed all 5 screens, CSV field mappings, JSON datastore, and port 8080 were per spec.
2. Analyze CLAUDE.md and create Projects screen — CLAUDE.md specifies a Project data model (project_id, project_description, attributes) from CSV. Implemented PROJECTS_PATH, parse_projects_csv(), load/save_projects(), three routes (/projects, /projects/upload, /projects/<id>/delete), projects.html, and "Projects" nav link.
3. Add ability to create Projects with attributes — Added POST /projects/create route accepting project_id, project_description, and dynamic attr_key[]/attr_value[] lists. Updated projects.html with a Create Project form featuring a JS-driven "+ Add Attribute" builder. 
4. Only show Project ID and Description in table; add Details button — Removed Attributes column from the projects table; added Details button linking to a new GET /projects/<id> route and project_detail.html template showing all fields and attributes.
5. Analyze CLAUDE.md for new Staffing feature — CLAUDE.md updated with Screen #6: monthly staffing profiles, employee matrixed across projects, future month planning, past month preservation. Analyzed data model, screens needed, and open design questions.
6. Proceed (implement Staffing) — Built data/staffing.json store, load/save_staffing(), 5 routes (/staffing, /staffing/<year>/<month>, /save, /copy, /new), staffing.html (month list with Current/Past/Planned badges), staffing_month.html (employee × project checkbox matrix, read-only past months, copy-to-next-month).
7. Add Supervisory Organization filter in Monthly staffing screen — Added client-side filter dropdown above the matrix populated from unique supervisor_organization values; filters rows instantly by org with a live count display.
8. Add secondary navigation to Staffing with 1 item linking to current Staffing page — Added single-item subnav ("Staffing") to staffing.html (active) and staffing_month.html (inactive); replaced back link.
9. Add Projections to Staffing navigation — tabular view with months as columns, employees as rows — Added GET /staffing/projections route and staffing_projections.html: employees as rows, current+future months as columns, project ID badges in each cell, Edit → links per month header. Added "Projections" to subnav on all Staffing pages. months as columns, project ID badges per cell, Edit → links per month header. Added "Projections" to subnav on all Staffing pages.
10. Add a color picker to Projects, JSON key is optional "Color" — Added <input type="color"> to create form; saved as color key; color swatch in projects table; header background and field row in project detail.
11. Update Project CSV import to map "Color" header to project_color — Added "Color": "project_color" to PROJECT_FIELD_MAP.
12. Show employee modal in Staffing Projections — Employee names became buttons opening a modal showing job profile, location, employee ID, and per-month project assignments.
13. Include Supervisory Organization in employee modal — Added supervisor_organization to JS data and modal body.
14. Format Staffing Projections columns "MM YYYY" — Column headers and modal month labels changed from full month name to zero-padded MM YYYY.
15. Remove Job Profile column from Staffing Projections — Removed <th> and <td> for Job Profile from the matrix table.
16. Include Supervisory Organization dropdown filter in Staffing Projections — Client-side filter bar above matrix with live count.
17. Format Staffing Projections columns "YYYY-MM" — Changed from MM YYYY to YYYY-MM in column headers and modal labels.
18. Color code project tags in Staffing Projections — Tags in matrix table and modal colored via project_color; luminance-based text contrast.
19. Add quick filters for projects in Staffing Projections — Project pill buttons toggle row visibility; combines with org filter; live count.
20. Add clear all project filters — Clear button appears when any project filter is active; hides when none selected.
21. Add Render button to Organization screen — Dark terminal-style modal renders org chart as ASCII tree with ├──/└── connectors; names in blue, metadata in green; reflects live drag-and-drop state.
22. Remove View Chart button and associated libraries — Removed button, .btn-chart style, GET /organization/chart route, and deleted organization_chart.html.
23. Update Project model — "Active" maps to active — Added to PROJECT_FIELD_MAP; parse_projects_csv coerces string to boolean ("1"/"true"/"yes" → true).
24. Update Projects screen with "active" checkbox — Checkbox (checked by default) in create form; Active column in table; Active field in detail page; projects_create saves as boolean.
25. Add Delete button to Projects screen — Inline delete form with confirmation per row.
26. Add Export to Projects screen — GET /projects/export exports projects.csv with all core fields + attributes; Export to CSV button in toolbar.
